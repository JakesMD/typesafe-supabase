name: typesafe_supabase
description: A dart package that enables type-safe queries to a Supabase database.
version: 0.0.2-dev.1
repository: https://github.com/JakesMD/typesafe-supabase
publish_to: "none"

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  meta: ^1.17.0
  supabase: ^2.8.0

  typesafe_postgrest:
    git: https://github.com/JakesMD/typesafe-postgrest.git

dev_dependencies:
  very_good_analysis: ^9.0.0
